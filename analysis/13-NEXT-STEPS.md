# 13 — Next Steps

This is the bridge between *analysis* and *generation*. Once Phase 0 (questions) is done, here is what we produce, in order.

## 13.1 The artifact pack we will produce

| File | Purpose | Generated when |
|---|---|---|
| `Kotlin APK/AGENTS.md` | Rules every coding agent (Codex, Antigravity, Claude Code, Cursor) must follow on this repo. | After Q-answers. |
| `Kotlin APK/MASTER-PROMPT.md` | Single big prompt you paste into Codex / Antigravity to scaffold the project, then iterate. | After Q-answers. |
| `Kotlin APK/AI/*.md` (15 files) | Documentation skeleton (mirrors KissKh `AI/`). | After Q-answers. |
| `Kotlin APK/update-codebase-graph.ps1` | Code-graph updater (your KissKh pattern). | After Q-answers. |
| `Kotlin APK/deploy_release.ps1` | Release deploy (your KissKh pattern, retargeted). | After Q-answers. |
| `Kotlin APK/.github/workflows/build.yml` | CI: assembleDebug + unit tests on PRs. | After Q-answers (optional). |
| `Kotlin APK/build.gradle.kts` (and friends) | Initial Gradle scaffold. | Generated by Codex/Antigravity using master prompt. |

## 13.2 AGENTS.md — outline (will be fleshed out post-answers)

The rules file will mirror your KissKh `agent rules` style and add Android-specific items:

```
0. GOLDEN RULES (NEVER VIOLATE)
   - Never start without explicit approval.
   - Never bump version on debug builds.
   - Always run ./gradlew lint and gradlew test before every build.
   - Always use ADB at <Q14 path>.
   - Always capture build + analyze logs under ./logs/.
   - Never request MANAGE_EXTERNAL_STORAGE.
   - Never enable GPL FFmpeg flags.
   - Never re-encode (always -c copy).
   - Never edit anything under analysis/, design/, or releases-repo/.

1. TOOLCHAIN
   - JDK 17 (Android Studio bundled).
   - Gradle wrapper.
   - NDK <pinned-version>.
   - FFmpeg artifact: <Q15>.

2. PLAN / MD APPROVAL GATE  (same as your other apps)

3. BUILD SYSTEM
   A. VERSION FORMAT
   B. DEBUG BUILD: ./gradlew assembleDebug -> install via ADB.
   C. RELEASE BUILD: AI/ docs gate + version-bump gate; ./gradlew assembleRelease.
   D. POST-BUILD VERIFY (install on device, list verification steps).

4. LINT / TEST GATE
   - ./gradlew lint and ./gradlew test before every build.
   - Errors: fix before build.
   - Warnings: ignored unless they hit Errors-as-warnings rules.
   - >=10 errors -> ISSUES.md gate (your pattern).

5. LOG CAPTURE
   - logs/<type>_<ts>.log, UTF-8.

6. ADB CONFIGURATION
   - <Q14 path>; check `adb devices` first; never wireless ADB unless asked.

7. ASKING QUESTIONS - mandatory scenarios (matches your other apps).

8. AI/ DOCUMENTATION GATE (release only) - matches KissKh.

9. DEPLOY - matches KissKh.

10. WORKFLOW SUMMARY (Quick reference).
```

## 13.3 Master prompt — outline

The master prompt for Codex / Antigravity will:

1. Set the role: "You are a senior Android engineer building MKV Slice."
2. Pin the rules: "Read and obey `AGENTS.md` before any change."
3. Pin the constraints from analysis docs (flat lossless, MKV out, FFmpeg-driven, foreground service, SAF-only).
4. Provide a checklist of phases (matching `12-ROADMAP.md`) and require one phase at a time with approval gates.
5. Reference Stitch design folder `<Q13>` for layouts; require pixel-faithful Compose translation.
6. Link `AI/` docs and require updates per the KissKh pattern when work completes.

This will be a ~3 000-word prompt. We don't paste it now; we generate after questions.

## 13.4 Order of operations after you answer Q1..Q24

1. I write `AGENTS.md` from the template.
2. I write `MASTER-PROMPT.md`.
3. I generate the `AI/` skeleton.
4. I copy/adapt `update-codebase-graph.ps1` and `deploy_release.ps1`.
5. I create `.gitignore`, top-level `README.md`.
6. I do **not** scaffold the actual Android project — that's for Codex / Antigravity using the master prompt, with you in the approval loop.
7. Once Codex finishes scaffolding, you import into Android Studio, hit `Sync`, and we move to Phase 4 (engine).

## 13.5 What I need *from you* to start Phase 1

- Your answers to [11-OPEN-QUESTIONS.md](11-OPEN-QUESTIONS.md) (or "go with defaults").
- A package name (Q11).
- A confirmation of the ADB path (Q14).
- The Stitch design folder path *if* it already exists (Q13); otherwise "deferred to Phase 2".

That's it. Once those land, I'll start writing AGENTS.md and the master prompt in this same workspace.

## 13.6 What I will NOT do until you greenlight

- Run any `gradle` command.
- Create any Android Studio project files.
- Touch your existing repos under `C:\Users\OYADLAPATI\source\repos\` other than the new `AI-LE\Kotlin APK\` subtree.
- Push anything to GitHub or any other remote.

These are deliberate — to match the "approval gate" rule from your other agent files.
